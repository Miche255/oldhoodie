const toggleMenu = () => document.body.classList.toggle("open");
/*const toggleMenu = () => {
    const initialIcon = document.querySelector('.initial-icon');
    const alternateIcon = document.querySelector('.alternate-icon');

    document.body.classList.toggle("open");

    if (document.body.classList.contains("open")) {
        initialIcon.style.display = "none";
        alternateIcon.style.display = "inline";
    } else {
        initialIcon.style.display = "inline";
        alternateIcon.style.display = "none";
        alternateIcon.classList.add('hidden'); // Agrega la clase oculta al icono de menú desplegable
    }
};*/







